// The client is a single-page app served as static files by the Go binary and,
// later, wrapped by Capacitor. There is no Node server to render on.
export const ssr = false;
export const prerender = false;
